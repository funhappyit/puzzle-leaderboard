server:
  port: 8081

spring:
  datasource:
    url: jdbc:mariadb://localhost:3307/puzzle_leaderboard?useUnicode=true&characterEncoding=utf8&restrictedAuth=mysql_native_password
    username: root
    password: root
    driver-class-name: org.mariadb.jdbc.Driver
  data:
    redis:
      host: localhost

      port: 6379
  jpa:
    show-sql: true

logging:
  level:
    com.example.leaderboard: DEBUG
